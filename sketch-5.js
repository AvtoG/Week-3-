function setup() { 
  createCanvas(400, 400);
}

function draw() { 
	y=200
	for(var x=50; x<=width; x+=100){
		for(var w=20; w<=80; w+=10){
			noFill()
			rectMode(CENTER)
			rect(x,y,w,w)
			
	}
}
}
